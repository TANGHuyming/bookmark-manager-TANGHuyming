# Environment variables for the blog app
# Copy this file to .env and fill in your values

PORT=3000
DOMAIN=localhost
NODE_ENV=development

# Mock admin token — used by adminAuth middleware to verify admin access
# In production, replace with a real session/JWT strategy
ADMIN_TOKEN=secret
